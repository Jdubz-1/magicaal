{
  "name": "@magicaal/caal-live",
  "version": "0.6.0",
  "private": true,
  "description": "Opt-in live smoke run for the Caal assistant — the CI corpus against a real provider",
  "scripts": {
    "start": "ts-node run.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
