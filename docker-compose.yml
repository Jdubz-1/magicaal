services:
  api-service:
    build:
      context: .
      dockerfile: apps/api-service/Dockerfile
    ports:
      - '${PORT:-3000}:3000'
    env_file:
      - apps/api-service/.env
    # Uncomment and configure infrastructure services as needed:
    # depends_on:
    #   db:
    #     condition: service_healthy
    #   redis:
    #     condition: service_healthy

  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: myapp
  #     POSTGRES_USER: myapp
  #     POSTGRES_PASSWORD: myapp
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U myapp"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  # redis:
  #   image: redis:7-alpine
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:
