// Must match Canvas.svelte's hard-coded <rect width="160" height="40">.
export const NODE_WIDTH = 160;
export const NODE_HEIGHT = 40;

// Minimum gap enforced between node boxes by collision avoidance and
// between ranks/nodes in the auto-layout.
export const MIN_CLEARANCE = 24;
